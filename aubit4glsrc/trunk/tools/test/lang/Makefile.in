
#Internationalization / language translation example

## ==================================================================
##                              Options
## ==================================================================

#All stuff common to more then one Aubit compiler make file is there:
include ../../incl/Makefile-common
override NOINCL=0

## ==================================================================
##                              Targets
## ==================================================================

lang: lang.4gl english.msg french.msg german.msg helpfile.msg italian.msg spanish.msg
# source.lang is generated by setting DUMPSTRINGS=ident
# then copying the resulting string.lang to source.lang (so it isn't overwritten every time you recompile)
	(export TRANSLATEFILE=source.lang;  ${FGLPCEXEC} lang.4gl -o lang)
	${MKMESS} english
	${MKMESS} french
	${MKMESS} german
	${MKMESS} italian
	${MKMESS} spanish
