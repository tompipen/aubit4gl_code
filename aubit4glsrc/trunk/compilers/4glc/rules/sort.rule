
sort_cmd : SORT variable_usage_expression KW_USING identifier {
		expr_str *callback;
		expr_str_list *params;
		if (!check_input_or_display_array_variable($<ptr>2)) {
			a4gl_yyerror("Variable is not a simple array");
			$<cmd>$=NULL;
		} else {
                	callback=A4GL_new_expr_fcall($<str>4,NULL,infilename,lastlineno, get_namespace($<str>4));
			$<cmd>$=new_sort_cmd($<ptr>2,callback);
		}
}
;

