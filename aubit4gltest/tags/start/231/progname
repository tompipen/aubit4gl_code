export A4GL_TRIMDUMP=24x80
./progname.4ae
if diff out out.expected  > /dev/null
then
	echo "Normal behaviour detected"
else

if diff out out.expected.scroll > /dev/null
then
	echo "SCROLLTOEND behaviour detected"
	:
else
	exit 1
fi
fi


exit 0
