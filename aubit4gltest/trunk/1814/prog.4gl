main
define d3 decimal(3)

initialize d3 to null

if d3 is null then
        display "d3 is null"
else
	exit program 1
end if


if d3 = 0  then
        display "decimal(3) = 0"
	exit program 1
else
	display "decimal(3) != 0"
end if

end main

