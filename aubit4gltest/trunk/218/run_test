./rpc_server > server.log &
lpid=$!
echo "Server : $lpid"
sleep 1
./rpc_client > client.log

kill $lpid 2> /dev/null 

if diff server.log server.log.expected
then
	:
else
	exit 1
fi

if diff client.log client.log.expected
then
	:
else
	exit 2
fi
exit 0
