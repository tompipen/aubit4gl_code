{**
 * @file
 * C code is stripped from generated .c file
 * @process TEST_GENERAL
 *}
 
MAIN
CODE
#include <stdio.h>
int nfiles;
int ret;
char *ptr;
int j;
char astring[10];
    ret = 0;
    if ( (astring[0]) ) {
        if (j >= 0)
            ret=6;
        else        {
            if (ret == 0)   {
                ptr = (char *)0;
                if (ptr == NULL) ret = -3;
            }
            if (ret == 0) {
                ++nfiles;
            }
        }
    }
ENDCODE
END MAIN
